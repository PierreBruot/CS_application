package com.coutinsociety.kanma.view.modelViewControler;

import android.app.AlertDialog;
import android.content.Context;
import android.content.DialogInterface;

import com.coutinsociety.kanma.view.MainActivity;

public class KanmaAlertDialog extends AlertDialog {
    public KanmaAlertDialog(final Context context) {
        super(context);
        /*
        this.setTitle("Alert");
        this.setMessage("Alert message to be shown");
        this.setButton(AlertDialog.BUTTON_POSITIVE, "OK",
                new DialogInterface.OnClickListener() {
                    public void onClick(DialogInterface dialog, int which) {
                        startAccountActivity();
                    }
                });
        this.setButton(AlertDialog.BUTTON_NEGATIVE, "Plus tard",
                new DialogInterface.OnClickListener() {
                    public void onClick(DialogInterface dialog, int which) {
                        dialog.dismiss();
                    }
                });*/
    }
}
